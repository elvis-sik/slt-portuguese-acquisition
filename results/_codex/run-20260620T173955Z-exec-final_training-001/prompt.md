Read `AGENTS.md` and verify that the scientific pilot passed and explicit human approval is recorded in the decision log. Freeze configs and data hashes. Run final behavioral trajectories in priority order: Portuguese seed A, shuffled Portuguese, matched English, Portuguese seed B. Use bounded jobs and checkpoint by target tokens. Evaluate every checkpoint and update the cost projection after each trajectory.

Do not inspect or optimize final LLC until behavior outputs and checkpoint selection rules are frozen. Stop if the projected total exceeds the hard budget or runtime gate.

Apply the scientific validity gate (`AGENTS.md`) before recording a trajectory as passed: confirm the structured-Portuguese condition's validation BPB **decreases** over training. If it increases, or the model degrades (English worsens with no Portuguese gain), this is a training/pipeline failure — return `status: blocked`, `gate_decision: pivot`, and route to diagnosis (learning rate/schedule, warmup, gradient clipping, token budget, scale, data). Do not freeze a degrading run and report it as a result.


---
## Orchestrator directive for this tick
- Time budget: 6.000 wall-clock hours (launch bounded jobs with --max-hours within this; poll and kill as you judge best).
- Task: Run the greenlit scale-up final_training using the FROZEN verified recipe from state/current_status.json: TinyStories-8M, full-parameter FP32 AdamW continued pretraining, lr 1e-4, 3% warmup, cosine decay, grad clip 1.0, weight_decay 0.01, batch 64, Wikipedia-PT corpus. Commit any required run/config state before launch, then run the long GPU job via infra/remote/run_bounded_job.sh with a bounded runtime. Train/evaluate the four priority conditions in order: structured Portuguese seed A, token-shuffled Portuguese, matched English, structured Portuguese seed B, targeting about 100,000,000 tokens per condition with dense log-spaced checkpoints (at least 15) and a fixed larger Portuguese validation set (at least 500 examples). Success criterion: a restartable final_training run under results/02_final_training/<run_id>/ with manifests/logs/status/final exit code, checkpoint hashes, package/hardware/precision/optimizer/data/tokenizer metadata, and updated state/current_status.json, state/decision_log.md, and state/experiment_registry.csv. Structured-PT validation BPB must decrease during training and beat the shuffled control. If structured PT degrades, English degrades with no Portuguese gain, or the scale-up contradicts the verified pilot, stop, preserve diagnostics, and return failed/blocked with gate_decision:pivot rather than reporting a null result.
